---
phase: 58-dead-ui-surfaces
plan: 01
subsystem: frontend, backend-api
tags: [F4, F5, F6, F7, integrity-gate, v0.9]
features: [F4, F5, F6, F7]
requires:
  - phase: 57-compliance-data-flow
provides:
  - Masking page Preview button wired to previewMasking
  - Subsetting DependencyGraph fed by discovery-store
  - Synthetic page lastConfigId wired via store after createConfig
  - RuleCreate/RuleUpdate Pydantic carries linked_column_ids + consistency_group
  - Masking rule editor consistency-group picker
status: in_progress
---

# Phase 58 Plan 01 — Dead-UI Surfaces + Linked-Column REST

## F4 — Masking Preview button
**Files:** `frontend/src/app/projects/[projectId]/masking/page.tsx`
- Add a Preview button next to existing actions on the policy detail view.
- Click handler: `await useMaskingStore.getState().previewMasking(projectId, selectedPolicyId, sampleSize=20)`.
- Show a loading state on the button while previewing; toast on error.
- Existing `<MaskingPreview data={previewData}/>` consumes the state automatically.

## F5 — Subsetting DependencyGraph wired
**Files:** `frontend/src/app/projects/[projectId]/subsetting/page.tsx`
- Import `useDiscoveryStore` to access `relationships` and `schemas`.
- On mount with a selected connection, ensure relationships are loaded.
- Derive `rootTables` from `selectedConfig?.root_table` (or `selectedConfig?.root_tables` if plural exists) — fall back to `[]` only when no config is selected.
- Pass `traversalDirection={selectedConfig?.traversal_strategy ?? "upstream"}` instead of hard-coded `"upstream"`.

## F6 — Synthetic lastConfigId
**Files:** `frontend/src/app/projects/[projectId]/synthetic/page.tsx`, `frontend/src/stores/synthetic-store.ts`
- Add `lastConfigId: string | null` field to `synthetic-store.ts` and a `setLastConfigId(id)` action.
- After `createConfig` succeeds, store sets `lastConfigId = newConfig.id`.
- Page reads `const lastConfigId = useSyntheticStore((s) => s.lastConfigId)`. Replace the dead `useState` line.

## F7 — Linked-column REST exposure
**Files:**
- `backend/app/api/v1/masking.py` — add fields to `RuleCreate` and `RuleUpdate` Pydantic models (look around lines 28-34 and 96-100). Fields: `linked_column_ids: list[uuid.UUID] | None = None`, `consistency_group: str | None = None`.
- `backend/app/application/masking/commands.py` + handlers — thread the new fields through `AddRuleCommand`/`UpdateRuleCommand` and into repo `add_rule`/`update_rule`. The repo's `_rule_to_entity` and `add_rule` paths should already accept them (migration 019 added the columns) — verify and wire if not.
- `backend/app/infrastructure/persistence/sqlalchemy/masking_repo.py` — confirm `add_rule` reads `rule.linked_column_ids` + `rule.consistency_group` from the entity and writes to the model. Add if missing.
- `backend/app/domain/masking/entities.py` — `MaskingRule` dataclass: ensure both fields exist with sane defaults (`linked_column_ids: list[uuid.UUID] = field(default_factory=list)`, `consistency_group: str | None = None`).
- `frontend/src/components/masking/masking-rule-editor.tsx` — add a small "Consistency group" text input (free-form label) and a multi-select for "Link to columns" (lists other columns in the same table; uses discovery-store data).
- `frontend/src/stores/masking-store.ts` — `addRule` / `updateRule` pass the new fields through.

## Tests
- **Backend:** extend `backend/tests/unit/test_masking_repo_round_trip.py` with a test verifying `linked_column_ids` + `consistency_group` round-trip through `add_rule` and `_rule_to_entity`.
- **Backend:** new integration test (or extend existing) verifying POST `/masking/policies/{id}/rules` with the new fields persists and surfaces them on GET.
- **Frontend:** if `tsc --noEmit` passes after the changes, that's the gate (frontend has no Jest tests today per the swarm evaluation).

## Acceptance
- `pytest backend/tests/unit -q` ≥ 339 passing.
- `pytest backend/tests/integration -q` 37+ passing (OIDC fail unchanged).
- `cd frontend && npx tsc --noEmit` clean.
- Manual: clicking Preview on a masking policy populates the preview pane. Subsetting graph renders edges. After generating a synthetic config, quality auto-loads. POSTing a rule with `linked_column_ids` round-trips via GET.
