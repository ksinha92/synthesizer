---
phase: 24-e2e-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/full-workflow.spec.ts
  - frontend/src/components/common/command-palette.tsx
  - frontend/src/components/layout/header.tsx
  - backend/tests/unit/test_masking_engine.py
  - backend/tests/unit/test_subsetting_engine.py
autonomous: true
---

<objective>
## Goal
Comprehensive E2E test covering all features, command palette (Cmd+K), additional unit tests for v0.2/v0.3 features, and final build verification — completing the v0.3 Enterprise milestone.

## Output
- Updated Playwright E2E covering all pages including masking, subsetting, workflows, compliance
- Command palette (Cmd+K global search)
- Additional backend unit tests for masking + subsetting engines
- Final build verification
</objective>

<acceptance_criteria>

## AC-1: E2E covers all major features
```gherkin
Given the full application
When Playwright E2E runs
Then it navigates: dashboard → projects → connections → discovery → synthetic → masking → subsetting → workflows → jobs → compliance → admin → 404
```

## AC-2: Command palette works
```gherkin
Given any page
When user presses Cmd+K
Then a search overlay appears with project/page navigation
```

## AC-3: Frontend builds clean
```gherkin
When npm run build is executed
Then it completes without errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update E2E, add command palette, add unit tests, verify build</name>
  <files>
    frontend/tests/e2e/full-workflow.spec.ts,
    frontend/src/components/common/command-palette.tsx,
    frontend/src/components/layout/header.tsx,
    backend/tests/unit/test_masking_engine.py,
    backend/tests/unit/test_subsetting_engine.py
  </files>
  <action>
    1. Update frontend/tests/e2e/full-workflow.spec.ts:
       - Add navigation to: masking, subsetting, workflows, compliance, admin, jobs
       - Verify each page renders without errors

    2. Create frontend/src/components/common/command-palette.tsx:
       - Cmd+K / Ctrl+K keyboard shortcut opens overlay
       - Search input with results: projects, pages (Dashboard, Discovery, Masking, etc.)
       - Arrow keys to navigate, Enter to select, Escape to close
       - Fuzzy text matching

    3. Update header.tsx: wire Cmd+K search placeholder to command palette

    4. Create backend/tests/unit/test_masking_engine.py:
       - test_hash_deterministic: same input + salt = same output
       - test_redact_replaces: output is all X's matching length
       - test_partial_mask: shows first/last chars
       - test_nullify: returns None

    5. Create backend/tests/unit/test_subsetting_engine.py:
       - test_build_graph: edges create correct adjacency
       - test_upstream_traversal: returns parent tables
       - test_cycle_detection: detects circular FKs
       - test_where_validation: rejects semicolons + DDL

    6. Verify: npm run build succeeds
  </action>
  <verify>
    - E2E test covers all pages
    - Cmd+K opens command palette
    - Backend unit tests pass
    - npm run build clean
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/24-e2e-polish/24-01-SUMMARY.md`
v0.3 ENTERPRISE MILESTONE COMPLETE
</output>
