---
phase: 36-polish-errors
plan: 01
type: execute
wave: 1
depends_on: ["35-01"]
files_modified:
  - frontend/src/components/common/toast.tsx
  - frontend/src/stores/*.ts (all 11 stores)
  - frontend/src/components/compliance/report-list.tsx
  - frontend/src/components/discovery/column-detail.tsx
autonomous: true
---

<objective>
Add toast notification system for mutation feedback, replace silent catch blocks in all stores with user-facing error state, fix compliance report summary parsing, and add save feedback to discovery column overrides.
</objective>

<acceptance_criteria>
## AC-1: Toast Notification System
Given any create/delete/update mutation, when it succeeds or fails, then a toast message appears briefly confirming the action or showing the error.

## AC-2: Store Error Handling
Given any API call fails in a store, when caught, then error is logged to console and an error toast is shown — no more silent swallowing.

## AC-3: Compliance Report Summary
Given a compliance report with summary data, when rendered in report-list, then summary stats are properly typed and safely accessed without `as any` casts.

## AC-4: Column Override Feedback
Given user saves a PII classification override, when save completes, then a success/error message is shown inline.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Toast + error handling + polish</name>
  <action>
    1. Create toast.tsx: lightweight toast component using a zustand store. Show/hide with auto-dismiss (3s). Support success/error/info variants.
    2. Update all 11 stores: replace silent catches with console.error + toast.error calls. Keep graceful fallback behavior (return null, set loading false) but add visibility.
    3. Fix report-list.tsx: type the summary field properly, remove `as any` casts, add safe access with optional chaining.
    4. Update column-detail.tsx: add inline success/error feedback after override save (brief "Saved!" message).
  </action>
</task>
</tasks>

<boundaries>
- DO NOT change store method signatures or return types
- Toast system must be minimal — no heavy library
- Keep existing error recovery behavior (return null, set loading false)
</boundaries>

<verification>
- npm run build clean
- No `as any` in report-list.tsx
- All stores log errors to console
- Toast renders on mutations
</verification>

<output>After completion, create summary.</output>
