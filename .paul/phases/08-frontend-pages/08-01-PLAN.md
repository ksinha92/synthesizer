---
phase: 08-frontend-pages
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/projects/page.tsx
  - frontend/src/app/projects/[projectId]/layout.tsx
  - frontend/src/app/projects/[projectId]/page.tsx
  - frontend/src/app/projects/[projectId]/connections/page.tsx
  - frontend/src/components/projects/project-list.tsx
  - frontend/src/components/projects/project-card.tsx
  - frontend/src/components/projects/create-project-dialog.tsx
  - frontend/src/components/connections/connection-list.tsx
  - frontend/src/components/connections/connection-card.tsx
  - frontend/src/components/connections/connection-form-drawer.tsx
  - frontend/src/components/dashboard/stat-card.tsx
  - frontend/src/components/dashboard/recent-projects.tsx
  - frontend/src/components/dashboard/active-jobs.tsx
  - frontend/src/components/common/empty-state.tsx
  - frontend/src/components/common/pagination.tsx
  - frontend/src/stores/project-store.ts
  - frontend/src/stores/connection-store.ts
autonomous: false
---

<objective>
## Goal
Build the Dashboard, Project List, and Connection management pages — the core navigation and data management UI — connected to real backend APIs via the useApi hook.

## Purpose
These are the first pages users interact with. Dashboard gives overview, Projects organizes work, Connections are the entry point for all TDM operations. Getting these right validates the full frontend → backend integration.

## Output
- Dashboard page with stat cards, recent projects, active jobs
- Projects list with table/card toggle, create dialog, pagination
- Project detail layout with sidebar sub-navigation
- Connections page with cards, status dots, slide-in form drawer, test button
- Reusable components: stat-card, empty-state, pagination
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/04-frontend-foundation/04-01-SUMMARY.md
(Uses: AppShell, useApi, auth-store, theme system, all layout components)
@.paul/phases/03-postgresql-connector/03-01-SUMMARY.md
(Uses: Project CRUD API, Connection CRUD + test + schemas API endpoints)

## Source Reference
@DATAWRANGLER.md (UI/UX Specification — Dashboard wireframe, Connection management, Page Specifications)

## Source Files
@frontend/src/components/layout/app-shell.tsx
@frontend/src/hooks/use-api.ts
@frontend/src/stores/auth-store.ts
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /aegis:audit | required | Before phase completion | ○ (deferred) |
</skills>

<acceptance_criteria>

## AC-1: Dashboard shows stat cards, recent projects, and active jobs
```gherkin
Given an authenticated user visits /
When the dashboard loads
Then stat cards show project count, active jobs, tables masked, jobs today (from API or placeholder)
And recent projects display as cards with name, table count, PII coverage %, last activity
And active jobs show progress bars with real-time status (placeholder for SSE — Phase 9)
And the PII coverage donut chart renders (Recharts)
And empty states show when no projects/jobs exist
```

## AC-2: Project list supports table/card view, create, and pagination
```gherkin
Given an authenticated user visits /projects
When the page loads
Then projects display in a table view by default (name, owner, connections, last activity)
And a card view toggle switches to visual cards
And "New Project" button opens a create dialog with name + description
And creating a project calls POST /api/v1/projects and adds to the list
And pagination shows page controls with total count
And empty state shows "Create your first project" with CTA button
```

## AC-3: Connection management has cards with status, form drawer, and test
```gherkin
Given an authenticated user visits /projects/{id}/connections
When the page loads
Then connections display as cards with connector icon, host/db, status dot (green/yellow/red/gray)
And "Add Connection" opens a slide-in drawer (480px) from right
And the form has connector type selector, dynamic fields per type, "Test Connection" button
And testing calls POST /connections/{id}/test and shows inline success/failure
And credentials fields are masked with show/hide toggle
And the drawer has progressive disclosure ("Advanced" collapsible for extra_params)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard page with stat cards, recent projects, and active jobs</name>
  <files>
    frontend/src/app/page.tsx,
    frontend/src/components/dashboard/stat-card.tsx,
    frontend/src/components/dashboard/recent-projects.tsx,
    frontend/src/components/dashboard/active-jobs.tsx,
    frontend/src/components/common/empty-state.tsx
  </files>
  <action>
    1. Create frontend/src/components/common/empty-state.tsx:
       - Reusable empty state: icon (Lucide), title, description, optional CTA button
       - Uses shadcn/Tailwind styling, centered layout
       - Adapts to light/dark theme

    2. Create frontend/src/components/dashboard/stat-card.tsx:
       - Card with: label (text-sm muted), value (text-2xl font-semibold), optional trend indicator
       - Clickable — accepts href prop for navigation
       - Hover elevation effect
       - Uses shadcn Card pattern (rounded-lg border bg-card shadow-sm)

    3. Create frontend/src/components/dashboard/recent-projects.tsx:
       - Fetches GET /api/v1/projects?page=1&page_size=6 via useApi
       - Displays project cards: name, description truncated, connection count, last updated relative time
       - "View All →" link to /projects
       - Empty state: "No projects yet. Create your first project."
       - Loading: skeleton cards

    4. Create frontend/src/components/dashboard/active-jobs.tsx:
       - Placeholder for now (real job list comes with Phase 9 SSE)
       - Shows static "No active jobs" empty state
       - Prepared for future: accepts jobs array prop, renders progress bars

    5. Update frontend/src/app/page.tsx:
       - Replace placeholder with real dashboard layout
       - Grid: 4 stat cards top row, recent projects + active jobs below, PII donut placeholder
       - Fetches stats from APIs (project count from /projects, others placeholder)
       - Wrapped in AppShell
       - Loading: skeleton stat cards + skeleton project cards

    Avoid: Fetching all data in a single API call — make parallel requests for each section.
    Avoid: Client-side date formatting without consistent timezone handling — use Intl.RelativeTimeFormat.
    Avoid: Rendering any user-provided string via dangerouslySetInnerHTML — always use React's default JSX escaping. This applies to project names, descriptions, connection names. <!-- audit-added: XSS prevention -->

    API fetch timeout: If any dashboard API call doesn't respond within 10 seconds, show an error state ("Unable to load") with a retry button instead of infinite skeleton. Use AbortController with setTimeout. <!-- audit-added: loading timeout -->
  </action>
  <verify>
    - Dashboard renders with stat cards, recent projects section, active jobs placeholder
    - Empty state shows when no projects exist
    - Stat cards are clickable
    - Loading skeletons appear before data loads
  </verify>
  <done>AC-1 satisfied: Dashboard shows stats, projects, jobs with empty states and loading.</done>
</task>

<task type="auto">
  <name>Task 2: Build Project List page with table/card views, create dialog, and pagination</name>
  <files>
    frontend/src/app/projects/page.tsx,
    frontend/src/app/projects/[projectId]/layout.tsx,
    frontend/src/app/projects/[projectId]/page.tsx,
    frontend/src/components/projects/project-list.tsx,
    frontend/src/components/projects/project-card.tsx,
    frontend/src/components/projects/create-project-dialog.tsx,
    frontend/src/components/common/pagination.tsx,
    frontend/src/stores/project-store.ts
  </files>
  <action>
    1. Create frontend/src/stores/project-store.ts (Zustand):
       - State: projects[], totalCount, currentPage, pageSize, viewMode ("table"|"card"), loading
       - Actions: fetchProjects(page), createProject(name, desc), deleteProject(id), setViewMode
       - API calls via the api convenience methods from use-api.ts

    2. Create frontend/src/components/common/pagination.tsx:
       - Page controls: Previous, page numbers, Next
       - Shows "Showing X-Y of Z"
       - Accepts: page, pageSize, totalCount, onPageChange

    3. Create frontend/src/components/projects/project-card.tsx:
       - Card view: project name, description (truncated), connection count badge, PII coverage % bar, last activity (relative time)
       - Click navigates to /projects/{id}
       - Hover: subtle elevation

    4. Create frontend/src/components/projects/project-list.tsx:
       - Dual view: table (Ant Design Table CSS-scoped) and card grid
       - Toggle button group: table icon / grid icon
       - Table columns: Name, Owner, Connections, Last Activity, Actions (edit/delete)
       - Card grid: 3 columns on desktop, 2 tablet, 1 mobile
       - Renders empty state when no projects

    5. Create frontend/src/components/projects/create-project-dialog.tsx:
       - shadcn Dialog with form: project name (required), description (optional)
       - Submit calls createProject from store
       - Loading state on submit button
       - Closes on success, shows error inline on failure

    6. Create frontend/src/app/projects/page.tsx:
       - Header: "Projects" + "New Project" button (opens create dialog)
       - View toggle (table/card)
       - ProjectList component
       - Pagination at bottom
       - Wrapped in AppShell

    7. Create frontend/src/app/projects/[projectId]/layout.tsx:
       - Project detail layout: AppShell with projectId passed (enables project-scoped sidebar nav)

    8. Create frontend/src/app/projects/[projectId]/page.tsx:
       - Project overview: name, description, stat cards (connections, discoveries, jobs)
       - Placeholder for full project dashboard

    Avoid: Re-fetching on every page navigation — Zustand store caches project list.
    Avoid: Ant Design Table outside .ant-scoped wrapper — always wrap in the CSS isolation container.
  </action>
  <verify>
    - /projects shows project list (table view default)
    - Toggle switches to card view
    - "New Project" opens dialog, creates project via API
    - Pagination works with page controls
    - /projects/{id} shows project detail with sidebar nav
    - Ant Design Table renders inside .ant-scoped container
    - Empty state shows "Create your first project"
  </verify>
  <done>AC-2 satisfied: Project list with table/card views, create dialog, pagination, empty state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dashboard and Project List pages connected to real backend APIs
  </what-built>
  <how-to-verify>
    1. Start backend: make dev (Docker Compose)
    2. Start frontend: cd frontend && npm run dev
    3. Visit http://localhost:3000 — verify dashboard with stat cards, empty project state
    4. Navigate to /projects — verify empty state with "Create your first project"
    5. Click "New Project" — create a project via dialog
    6. Verify project appears in list (table view)
    7. Toggle to card view — verify card rendering
    8. Click project — verify /projects/{id} with sidebar nav
    9. Test dark mode on all pages
    10. Test responsive (mobile width) — verify layout adapts
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Build Connection management with cards, form drawer, and test</name>
  <files>
    frontend/src/app/projects/[projectId]/connections/page.tsx,
    frontend/src/components/connections/connection-list.tsx,
    frontend/src/components/connections/connection-card.tsx,
    frontend/src/components/connections/connection-form-drawer.tsx,
    frontend/src/stores/connection-store.ts
  </files>
  <action>
    1. Create frontend/src/stores/connection-store.ts (Zustand):
       - State: connections[], loading, testResults (map of connection_id → {success, latency})
       - Actions: fetchConnections(projectId), createConnection(data), testConnection(id), deleteConnection(id)
       - API calls to /projects/{pid}/connections endpoints

    2. Create frontend/src/components/connections/connection-card.tsx:
       - Card with: connector type icon (Database/Cloud icons from Lucide), connection name
       - Host/database display (e.g., "postgres://db-prod:5432/claims")
       - Status dot: green (connected), yellow (untested), red (failed), gray (disabled)
       - Last tested timestamp (relative)
       - Action buttons: Test, Edit, Delete
       - Test button: calls testConnection, shows inline spinner → success/failure result

    3. Create frontend/src/components/connections/connection-form-drawer.tsx:
       - Slide-in drawer from right, 480px width (shadcn Sheet component)
       - Connector type selector: 4 options (PostgreSQL, MySQL, MongoDB, Snowflake) with icons
       - Dynamic form fields based on selected type:
         - SQL (PG/MySQL): host, port, database, username, password
         - MongoDB: host, port, database, username, password
         - Snowflake: account, warehouse, database, username, password, role
       - Password field: masked by default with eye icon toggle
       - "Advanced" collapsible section for extra_params (JSON textarea)
       - "Test Connection" button: validate required fields (host, port, database, username) BEFORE calling API — show inline validation errors if empty, don't send request <!-- audit-added: client-side validation before test -->
       - "Save" button: calls create/update endpoint
       - Progressive disclosure: only essential fields visible, advanced collapsed
       - Validation: required fields highlighted, inline error messages on blur and on submit

    4. Create frontend/src/components/connections/connection-list.tsx:
       - Grid of connection cards (2 columns desktop, 1 mobile)
       - Empty state: "Add your first connection" with icon + CTA

    5. Create frontend/src/app/projects/[projectId]/connections/page.tsx:
       - Header: "Connections" + "Add Connection" button
       - ConnectionList component
       - "Add Connection" opens ConnectionFormDrawer
       - Wrapped in project layout (sidebar shows project nav)

    Avoid: Displaying raw credentials — password field always masked, API never returns passwords.
    Avoid: Letting drawer state leak between connections — reset form on close.
  </action>
  <verify>
    - /projects/{id}/connections shows connection list
    - "Add Connection" opens drawer from right
    - Connector type selector changes form fields dynamically
    - Password field is masked with toggle
    - "Test Connection" calls API and shows result inline
    - Save creates connection via API
    - Connection cards show correct status dots
    - Empty state shows when no connections
  </verify>
  <done>AC-3 satisfied: Connection management with cards, form drawer, test, progressive disclosure.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- backend/ (all backend code — stable)
- frontend/src/components/layout/ (Phase 4 — stable AppShell)
- frontend/src/components/theme/ (Phase 4 — stable theme system)
- frontend/src/components/ui/error-boundary.tsx (Phase 4 — stable)
- frontend/src/app/layout.tsx (Phase 4 — stable root layout)
- frontend/src/app/(auth)/login/page.tsx (Phase 4 — stable)

## SCOPE LIMITS
- Dashboard + Projects + Connections ONLY (Plan 01)
- Schema Explorer, PII Results, Synthetic Generation in Plan 02
- No Job monitoring UI (Phase 9 — SSE not built yet)
- No onboarding wizard (Plan 02 or Phase 10)
- No AI assistant implementation (deferred — scaffold exists in header)
- No command palette implementation (deferred — placeholder exists)
- PII donut chart on dashboard is a placeholder (needs discovery data from Plan 02)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Dashboard renders with stat cards and project section
- [ ] /projects shows list with table/card toggle
- [ ] Create project dialog works end-to-end (API call succeeds)
- [ ] /projects/{id}/connections shows connection cards
- [ ] Connection form drawer opens, dynamic fields work per connector type
- [ ] Test Connection calls API and shows result
- [ ] Empty states display correctly on all pages
- [ ] Dark mode renders correctly on all new pages
- [ ] Ant Design Table CSS-scoped (no Tailwind conflicts)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 auto tasks + 1 checkpoint completed
- Full frontend → backend integration working (create project, add connection, test)
- UI matches DATAWRANGLER.md wireframes
- Responsive on desktop and mobile
</success_criteria>

<output>
After completion, create `.paul/phases/08-frontend-pages/08-01-SUMMARY.md`
Note: Phase 8 continues with Plan 02 (Discovery + PII + Synthetic pages)
</output>
