---
phase: 35-masking-discovery
plan: 01
type: execute
wave: 1
depends_on: ["34-01"]
files_modified:
  - frontend/src/app/projects/[projectId]/masking/page.tsx
  - frontend/src/stores/masking-store.ts
  - frontend/src/app/projects/[projectId]/discovery/page.tsx
  - frontend/src/stores/discovery-store.ts
  - frontend/src/components/synthetic/quality-report.tsx
  - frontend/src/components/workflows/workflow-editor.tsx
autonomous: true
---

<objective>
Complete masking rule application per column, wire relationship graph data from discovery API, fix quality report distribution charts to use real data, and upgrade workflow editor from raw JSON to a proper structured form.
</objective>

<acceptance_criteria>
## AC-1: Masking Rule Application
Given auto-suggest results, when user clicks "Apply" on a suggestion, then a rule is added to the selected policy with that column + strategy. Add ability to apply individual suggestions or all at once.

## AC-2: Relationship Graph from API
Given discovery schemas exist, when user views the Relationship Graph tab, then FK relationships are fetched from the API and displayed as edges in the graph.

## AC-3: Quality Report Real Data
Given a quality evaluation result, when rendered, then distribution charts show actual column distributions from the API response (not hardcoded data). The composite score, column scores, and privacy metrics all come from the API.

## AC-4: Workflow Editor Structured Form
Given the workflow creation dialog, when user creates a workflow, then they can add nodes from a dropdown (discover/mask/generate/subset/export), connect them sequentially, and see a visual preview — instead of editing raw JSON.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Complete masking + discovery + synthetic + workflow</name>
  <action>
    1. Masking store: add addRule method (POST /masking/policies/{policyId}/rules with column_id, strategy, config).
    2. Masking page: add "Apply" button per suggestion that calls addRule. Add "Apply All" button. Show applied rules with checkmarks. Wire suggestions to selected policy.
    3. Discovery store: add fetchRelationships method that returns FK relationships from schema data.
    4. Discovery page: fetch relationships when graph tab is active, pass to RelationshipGraph component instead of empty array.
    5. Quality report: ensure distribution chart data comes from the API response column_scores, not hardcoded buckets. If API returns column distributions, map them to chart data.
    6. Workflow editor: replace raw JSON textarea with structured form — node list with add/remove, type dropdown per node, auto-generate edges from sequential order. Show visual node chain preview.
  </action>
</task>
</tasks>

<boundaries>
- DO NOT change RelationshipGraph component internals (it already accepts tables + relationships props)
- DO NOT modify store interfaces beyond what's needed
- Masking rule application must work with existing policy API
</boundaries>

<verification>
- npm run build clean
- Masking page shows Apply button per suggestion
- Relationship graph tab fetches and displays edges
- Quality report renders real data from API
- Workflow editor has structured form instead of raw JSON
</verification>

<output>After completion, create summary.</output>
