---
phase: 30-preview-heatmap
plan: 01
type: execute
wave: 1
depends_on: ["25-01", "26-01"]
files_modified:
  - frontend/src/components/dashboard/pii-heatmap.tsx
  - frontend/src/components/jobs/job-gantt.tsx
  - frontend/src/components/masking/masking-preview.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/projects/[projectId]/jobs/page.tsx
autonomous: true
---

<objective>
Build PII heatmap for dashboard, Job Gantt timeline view, and enhance masking side-by-side preview — competitor parity features.
</objective>

<acceptance_criteria>
## AC-1: PII heatmap on dashboard
Given discovered PII data, when dashboard loads, then a CSS grid shows tables as cells with color intensity = PII density, click drills into table.

## AC-2: Job Gantt timeline
Given job history, when user toggles "Timeline" on jobs page, then Recharts horizontal bar chart shows temporal overlap.

## AC-3: Enhanced masking preview
Masking preview shows color diff highlighting on changed values.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Build heatmap + Gantt + enhanced preview</name>
  <action>
    1. Create pii-heatmap.tsx: CSS grid, each cell = table name, bg-opacity proportional to PII column %, unmasked count shown, click navigates to discovery.
    2. Create job-gantt.tsx: Recharts BarChart horizontal, bars = jobs by start/end time, color by type, toggle between list/timeline on jobs page.
    3. Update masking-preview.tsx: add subtle bg-amber-100 on masked values that differ from original (color diff highlighting).
    4. Add heatmap to dashboard below donut.
    5. Add timeline toggle to jobs page.
  </action>
</task>
</tasks>

<output>After completion, create summary.</output>
