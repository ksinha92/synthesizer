---
phase: 31-form-validation
plan: 01
type: execute
wave: 1
depends_on: ["30-01"]
files_modified:
  - frontend/src/components/common/form-field.tsx
  - frontend/src/components/common/skeleton-card.tsx
  - frontend/src/components/connections/connection-form-drawer.tsx
  - frontend/src/components/projects/create-project-dialog.tsx
  - frontend/src/components/synthetic/config-form.tsx
  - frontend/src/components/projects/project-list.tsx
  - frontend/src/components/subsetting/subsetting-config.tsx
autonomous: true
---

<objective>
Standardize all forms with react-hook-form + zod schemas, replace remaining native HTML tables with Ant Design Table, and add skeleton loading screens on key pages.
</objective>

<acceptance_criteria>
## AC-1: Form Validation Standardization
Given forms across the app, when user interacts, then react-hook-form + zod handles validation with consistent error display. Target: connection form drawer, create project dialog, synthetic config form. All forms must submit identical payloads to the same store methods as current code. Connection form "Test Connection" save-then-test flow must work. Form reset on cancel/close must work.

## AC-2: Skeleton Loading Screens
Given any page that fetches data, when loading, then a skeleton placeholder (pulse animation matching the content shape) appears instead of spinners or bare text. No layout shift on data arrival.

## AC-3: Ant Design Table Migration
Given native HTML tables (project list table-view branch, subsetting analysis), when rendered, then they use Ant Design Table with sort/filter where appropriate, wrapped in .ant-scoped. Project-list card view remains untouched — only the table-view branch is migrated.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Form + skeleton + table upgrades</name>
  <action>
    1. Create form-field.tsx: reusable wrapper integrating react-hook-form Controller with consistent label + error display. Must support field types: text, number, password (with optional show/hide toggle), textarea, and select. Accept `control` prop from `useForm()`. Wire `aria-invalid={!!error}` and `aria-describedby={errorId}` on inputs. Error messages render with `role="alert"`. <!-- audit-added: M2, S1 -->
    2. Create skeleton-card.tsx: reusable skeleton component (pulse div variants for cards, rows).
    3. Migrate connection-form-drawer.tsx to react-hook-form + zod schema. Zod schema: name non-empty trimmed, host required when not Snowflake, port integer 1-65535, databaseName non-empty, username non-empty, account required when Snowflake, warehouse required when Snowflake, extraParams validated as parseable JSON with .refine(). Password field retains show/hide toggle. Save-then-test flow preserved. Use z.transform(v => v.trim()) on string fields. <!-- audit-added: M3, S2, S3 -->
    4. Migrate create-project-dialog.tsx to react-hook-form + zod. Schema: name non-empty trimmed string (min 1, max 100), description optional string (max 500). Use z.transform(v => v.trim()) on name. <!-- audit-added: M3, S2 -->
    5. Migrate synthetic config-form.tsx to react-hook-form + zod. Schema: connectionId non-empty, rowCount integer 1-100000, seed optional number. <!-- audit-added: M3 -->
    6. Replace project-list.tsx table-view branch only with Ant Design Table (name, description, updated, actions columns). Card view grid remains untouched. <!-- audit-added: M5 -->
    7. Replace subsetting-config.tsx analysis results native table with Ant Design Table.
    8. Add skeleton loading to project-list, job-list, and dashboard stat cards.
  </action>
</task>
</tasks>

<boundaries>
- DO NOT change component APIs or store interfaces
- DO NOT modify pages that already use Ant Design Table (pii-results-table, audit-log-viewer)
- DO NOT migrate rbac-manager.tsx — it is a placeholder with static data; leave for real RBAC implementation <!-- audit-added: M1 -->
- Keep existing functionality identical — validation is additive
- Preserve project-list card view toggle — only table-view branch migrated <!-- audit-added: M5 -->
- All zod schemas must produce inferred types (z.infer<typeof schema>) used in useForm generic parameter <!-- audit-added -->
</boundaries>

<verification>
- npm run build clean with zero new TypeScript warnings
- All forms show field-level errors on invalid input with aria-invalid and role="alert" <!-- audit-added: S1 -->
- All 3 migrated forms submit identical payload shape to same store methods as before <!-- audit-added: M4 -->
- Connection form test-connection flow works, form reset on cancel/close works <!-- audit-added: M4 -->
- Project-list card view toggle still works after table migration <!-- audit-added: M5 -->
- Ant Design tables render inside .ant-scoped wrapper
- Loading states show skeletons without layout shift
</verification>

<output>After completion, create summary.</output>
