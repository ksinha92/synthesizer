---
phase: 37-testing-deployment
plan: 01
type: execute
wave: 1
depends_on: ["36-01"]
files_modified:
  - backend/tests/unit/test_masking_engine.py
  - backend/tests/unit/test_quality_evaluator.py
  - backend/tests/integration/test_auth_flow.py
  - backend/tests/integration/test_masking_api.py
  - frontend/tests/e2e/user-journeys.spec.ts
  - docker-compose.prod.yml
  - docs/DEPLOYMENT.md
  - docs/RUNBOOK.md
autonomous: true
---

<objective>
Expand backend test coverage (masking engine, quality evaluator, auth flow, masking API), add comprehensive E2E user journey tests, validate Docker prod deployment config, and create operations runbook + deployment guide for Ameritas ops team.
</objective>

<acceptance_criteria>
## AC-1: Backend Test Expansion
Given existing test suite, when new tests are added, then masking engine (7 strategies), quality evaluator (composite score), auth flow (login/logout/token refresh), and masking API (CRUD + execute) are covered. All tests pass with pytest.

## AC-2: E2E User Journey Tests
Given Playwright setup, when tests run, then complete user journeys are covered: create project → add connection → run discovery → apply masking → view jobs. Mobile viewport tests included.

## AC-3: Docker Prod Validation
Given docker-compose.prod.yml, when reviewed, then all services have health checks, resource limits, proper dependency ordering, and .env.prod has all required variables documented.

## AC-4: Operations Documentation
Given ops team needs, when docs are created, then DEPLOYMENT.md has step-by-step deploy guide and RUNBOOK.md has troubleshooting, monitoring, backup/restore, and credential rotation procedures.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Tests + deployment + docs</name>
  <action>
    1. Create test_masking_engine.py: test all 7 masking strategies (hash, redact, faker_replace, shuffle, nullify, fpe, partial_mask) with sample data.
    2. Create test_quality_evaluator.py: test composite score calculation, KS test, correlation check.
    3. Create test_auth_flow.py: test login redirect, callback, token validation, logout.
    4. Create test_masking_api.py: test policy CRUD, rule creation, preview, execute endpoints.
    5. Create user-journeys.spec.ts: Playwright tests covering full create→discover→mask flow, Gantt toggle, theme switch, mobile sidebar.
    6. Review and fix docker-compose.prod.yml if needed (verify healthchecks, ordering, limits).
    7. Create docs/DEPLOYMENT.md: prerequisites, env setup, build steps, deploy commands, verify checklist.
    8. Create docs/RUNBOOK.md: common issues, log locations, restart procedures, backup/restore, credential rotation, monitoring setup.
  </action>
</task>
</tasks>

<boundaries>
- DO NOT modify backend application code — tests only
- DO NOT modify frontend components — E2E tests only
- Documentation must be practical, not theoretical
</boundaries>

<verification>
- npm run build clean (frontend)
- Backend tests discoverable by pytest (valid Python)
- Playwright tests valid TypeScript
- Docs are complete and actionable
</verification>

<output>After completion, create summary.</output>
