---
phase: 25-wire-missing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/components/layout/app-shell.tsx
  - frontend/src/components/dashboard/stat-card.tsx
  - frontend/src/components/dashboard/pii-donut.tsx
  - frontend/src/components/discovery/pii-results-table.tsx
  - frontend/src/components/admin/audit-log-viewer.tsx
  - frontend/src/components/jobs/job-list.tsx
autonomous: true
---

<objective>
## Goal
Fix dead code (assistant sidebar), install missing dependencies (recharts, react-hook-form, zod), bind real API data to dashboard, and replace 3 native HTML tables with Ant Design Table — unblocking all subsequent v0.4 phases.

## Purpose
The UI/UX assessment found critical wiring issues: the AI assistant is built but never rendered, Recharts isn't installed (blocks all chart work), and Ant Design Table is installed but never used. These are 1-day fixes with outsized impact.

## Output
- AssistantSidebar rendered on every page
- Recharts, react-hook-form, zod installed
- Dashboard stat cards bound to real API data
- PII coverage donut chart (Recharts)
- 3 Ant Design Tables replacing native HTML tables
</objective>

<context>
## Assessment findings
- assistant-sidebar.tsx exists but never imported into layout
- recharts not in package.json
- antd installed, .ant-scoped CSS defined, zero Ant components rendered
- Dashboard stat values hardcoded to "—"
</context>

<acceptance_criteria>

## AC-1: Assistant sidebar renders and toggles from header
```gherkin
Given any page in the app
When the user clicks the Bot icon in the header
Then the AssistantSidebar slides in from the right (400px)
And it shows the consent banner on first open
And messages can be sent (calls /assistant/chat API)
```

## AC-2: Dashboard shows real data from API
```gherkin
Given an authenticated user on the dashboard
When the page loads
Then stat cards show real counts (projects, active jobs, tables masked)
And a PII coverage donut chart renders via Recharts (detected/masked/pending)
```

## AC-3: Ant Design Tables render in CSS-scoped containers
```gherkin
Given the PII results, audit log, or job list page
When the table loads
Then Ant Design Table renders with sorting + filtering
And the table is wrapped in .ant-scoped (no Tailwind CSS conflicts)
And dark mode works correctly on Ant components
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + wire assistant sidebar</name>
  <files>
    frontend/package.json,
    frontend/src/app/layout.tsx,
    frontend/src/components/layout/app-shell.tsx
  </files>
  <action>
    1. Install dependencies in package.json:
       - recharts (charts library)
       - react-hook-form + @hookform/resolvers + zod (form validation)

    2. Wire AssistantSidebar into app-shell.tsx (NOT layout.tsx — needs projectId context):
       - Import AssistantSidebar from @/components/assistant/assistant-sidebar
       - Render <AssistantSidebar projectId={projectId} /> inside AppShell
       - This gives the assistant access to which project the user is viewing
       - For pages without projectId (dashboard, admin), pass undefined

    3. Verify header Bot button toggles sidebar open/closed

    Avoid: Adding assistant to layout.tsx root — it needs AppShell's projectId prop.
  </action>
  <verify>
    - `npm install` succeeds with new dependencies
    - Bot button in header opens/closes assistant panel
    - Consent banner appears on first open
  </verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard real data + PII donut chart</name>
  <files>
    frontend/src/app/page.tsx,
    frontend/src/components/dashboard/stat-card.tsx,
    frontend/src/components/dashboard/pii-donut.tsx
  </files>
  <action>
    1. Update dashboard page.tsx:
       - Fetch project count: GET /api/v1/projects?page=1&page_size=1 → use total_count
       - Fetch job stats: GET /api/v1/projects/{first_project}/jobs?status=running → count
       - Display real numbers in stat cards (replace "—")
       - Handle loading state (skeleton) and error state

    2. Create frontend/src/components/dashboard/pii-donut.tsx:
       - Use Recharts PieChart + Pie + Cell
       - 3 segments: Detected (red), Masked (green), Pending (yellow)
       - Center label: total PII count
       - Placeholder data for now (will bind to discovery API in Phase 26)
       - Responsive: 200px diameter on desktop, smaller on mobile

    3. Add PII donut to dashboard page below stat cards

    Avoid: Fetching data from ALL projects — fetch from first project only for dashboard summary.
  </action>
  <verify>
    - Dashboard stat cards show numbers (not "—")
    - PII donut chart renders with Recharts (3 colored segments)
    - Loading skeletons show before data arrives
  </verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Replace 3 native HTML tables with Ant Design Table</name>
  <files>
    frontend/src/components/discovery/pii-results-table.tsx,
    frontend/src/components/admin/audit-log-viewer.tsx,
    frontend/src/components/jobs/job-list.tsx
  </files>
  <action>
    1. Update pii-results-table.tsx:
       - Replace native <table> with Ant Design <Table>
       - Wrap in <div className="ant-scoped"> for CSS isolation
       - Add columns config: Table, Column, PII Type (badge render), Confidence (progress bar render), Detector, Status, Actions
       - Enable: sorting on confidence, filtering on PII type + classification
       - Pagination built-in via Ant Table

    2. Update audit-log-viewer.tsx:
       - Replace native <table> with Ant Design <Table>
       - Wrap in .ant-scoped
       - Add columns: Time, User, Action, Resource, Status (color tag), IP
       - Enable: sorting on time, filtering on action type

    3. Update job-list.tsx:
       - This one uses cards, not a table — keep card layout but add Ant Design Table as an alternative "table view" toggle (like projects page has table/card toggle)
       - Table columns: Type (icon), Status (tag), Progress (progress bar), Started, Duration, Actions

    All Ant Tables: ensure ConfigProvider theme sync works in dark mode.

    Avoid: Removing the .ant-scoped wrapper — it prevents Tailwind CSS conflicts.
  </action>
  <verify>
    - PII results page shows Ant Design Table with sort/filter
    - Audit log shows Ant Design Table with sort
    - Both tables render correctly in dark mode
    - No CSS conflicts between Tailwind and Ant Design
  </verify>
  <done>AC-3 satisfied.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- All backend code (stable)
- All existing component logic (only replace table rendering, don't change data flow)
## SCOPE LIMITS
- Dashboard data binding is approximate (first project only)
- PII donut uses placeholder data (real binding in Phase 26)
- Only 3 tables replaced with Ant Design (rest stay native HTML)
</boundaries>

<verification>
- [ ] `npm install` + `npm run build` succeeds
- [ ] Assistant sidebar opens from header Bot button
- [ ] Dashboard stat cards show real numbers
- [ ] PII donut renders with Recharts
- [ ] Ant Design Tables render in .ant-scoped without CSS conflicts
- [ ] Dark mode works on all modified components
</verification>

<output>
After completion, create `.paul/phases/25-wire-missing/25-01-SUMMARY.md`
</output>
