"""File writers for the v0.7 synthetic file output pipeline.

Each writer consumes a FileSchemaDefinition + row dicts and emits one file
in the corresponding format. See base_writer.BaseFileWriter for the contract.
"""
